-##map-canvas
-#:javascript
-#  function initialize() {
-#    var mapOptions = {
-#      center: { lat: -34.397, lng: 150.644},
-#      zoom: 8
-#    };
-#    var map = new google.maps.Map(document.getElementById('map-canvas'),
-#        mapOptions);
-#  }
-#  google.maps.event.addDomListener(window, 'load', initialize);
-#%script{:src => 'https://maps.googleapis.com/maps/api/js?key=AIzaSyCwMPYWQv-RasZnNTRJJwrzMWHZWQpW4E0', :type => 'text/javascript'}

#phototag
  .container
    .row
      .text-center
        - if item.picture_url.to_s.empty? 
          %img{src:'http://placehold.it/500x300', class:'img-thumbnail'}
        - else
          = image_tag item.picture_url, class:'img-thumbnail'
    .row

      .form-group
        = form_tag :photo_tags, method: 'puts', id:'playform' do
          #tags
          = hidden_field_tag :item, 1
          = submit_tag 'submit'
